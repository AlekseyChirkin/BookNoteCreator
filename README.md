# Book Note Creator

Портативное десктоп-приложение для добавления прочитанных книг из **litres.ru** и **author.today** в Obsidian-совместимые Markdown-файлы с обложками.

## Быстрый старт (Windows)

Рекомендуемый способ — использование портативной папки:

1. Запустите `build_portable.bat` (из корня проекта)
2. Перейдите в папку `app/`
3. Запустите `start.bat`
4. Браузер откроется автоматически по адресу `http://127.0.0.1:5000`

### Что внутри портативной папки (`app/`)

| Файл / Папка | Описание |
|---|---|
| `BookNoteCreator.exe` | Собранный исполняемый файл (PyInstaller) |
| `start.bat` | Точка входа — ланчер приложения |
| `playwright-browsers/` | Встроенный Chromium для парсинга author.today |
| `_internal/` | Шаблоны, статика и ресурсы приложения |
| `_resources/` | Папка сгенерированных `.md` файлов и обложек |

Скопируйте всю папку `app/` на другой ПК — запустите `start.bat`, приложение работает автономно.

## Режим разработки

### Требования

- Python 3.10+
- Windows 10/11
- Git (для проверки)

### Установка

```powershell
cd "D:\Projects\BookNoteCreator - portable"
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
playwright install chromium
```

### Запуск

```bash
python web_book_parser.py
```

Сервер запустится на `http://127.0.0.1:5000` и автоматически откроет браузер.

## Использование

1. Вставьте URL книги (litres.ru или author.today) в поле ввода
2. Выберите дату прочтения
3. При необходимости установите личный рейтинг, рекомендацию и комментарий
4. Нажмите **Сохранить книгу**

Сохранённые файлы пишутся в `_resources/` рядом с приложением.

### Защита от дубликатов

Проверка основана на URL книги: если уже существует `.md` файл с этим URL — дубликат не создаётся, пользователю отображается уведомление.

## Архитектура проекта

| Файл | Назначение |
|---|---|
| `web_book_parser.py` | Flask-приложение и API маршруты (`/parse`, `/save`) |
| `parser.py` | Парсер метаданных книг (JSON-LD, CSS-селекторы, Playwright) |
| `output_generator.py` | Генерация Markdown-frontmatter и обложек |
| `input_parser.py` | Разбор входного `.md` файла со списком книг |
| `app_paths.py` | Пути к ресурсам (portable/dev режимы) |
| `templates/index.html` | Фронтенд: форма ввода, превью книги, визуальные элементы |
| `static/style.css` | CSS-стили фронтенда, адаптивный дизайн, тёмная тема |
| `build_portable.bat` | Сборка портативной версии через PyInstaller |

## Поддерживаемые сайты

### author.today
- Парсинг через Playwright (обход JS-рендеринга)
- Извлечение: название с номером тома, авторы, серия, аннотация, обложка, рейтинг (лайки), теги, аудиокниги
- Фоллбэк на `requests` при отсутствии Playwright

### litres.ru
- Парсинг через статичный HTML (`requests`)
- JSON-LD + CSS-селекторы для извлечения метаданных
- Извлечение: название, авторы, серия, номер тома, аннотация, обложка, рейтинг, жанги, чтец

## Формат записи в Obsidian

```yaml
---
date: 2025-12-01
title: Название книги
author:
  - Автор 1
  - Автор 2
series: Название серии
rating: 👍 97 / ⭐️⭐️⭐️⭐️⭐️
useful: yes
status: Finished
type: Book
cover: "Autor_-_Title.png"
URL: https://author.today/work/622629
---

#Book, #Author1, #SeriesName
[[Autor_-_Title.png]]

Комментарий к прочитанному: ...

Жанр: Фантастика, Приключения

Аннотация:
Текст аннотации...
```

## Примечания

- `author.today` требует Playwright с установленным Chromium. Портативная сборка включает Chromium встроенно.
- Если Playwright недоступен — парсинг author.today откатится на статичный HTML (качество ниже), а litres.ru останется рабочим.
- Пакетный импорт был исключён для упрощения сборки и стабильности.
- Сборку `build/` и `dist/` можно удалить после успешного копирования — это артефакты PyInstaller.

## Лицензия

MIT
