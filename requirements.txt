# Фреймворк веб-сервера
flask>=3.0.0           # Веб-сервер и маршрутизация (REST API для фронтенда)

# Парсинг HTML-страницы
beautifulsoup4>=4.12.0 # Разбор HTML/DOM (поиск элементов по селекторам, JSON-LD)
lxml>=4.9.0            # Быстрый парсер XML/HTML для BeautifulSoup

# HTTP-запросы
requests>=2.31.0       # HTTP-клиент для статичного HTML и загрузки обложек

# Динамический JS-рендеринг (для author.today)
playwright>=1.50.0     # Автоматизация браузера Chromium для JS-страниц
